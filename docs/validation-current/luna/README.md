# GPT-5.6 Luna contract model validation

- `catalog.json`: exact model entry from public live Gateway catalog, no inference.
- `live-handler.json`: two real Luna calls using the existing handler, authenticated production app identity and shared verification quota. Includes provider-returned model, token/cache details and a separately settled 66-microdollar cache-write accounting correction. No mock or browser signature claim.
- `production/result.json` and screenshots: three actual deployed Luna proposals and two explicit signatures via native Windows Edge 153 controls, 440x956 touch emulation, WebGPU, real clock. Left→right correction, two bullets erased, then slow bullets/charged reflection and preserved HP/boss/time. No fake media or human-microphone input.
- `result.json`: combined source/deployment, 209-test result, rates/settings, measured latency, conservative costs and evidence limitations.

All existing human recordings and the Windows Vivaldi success report preceded the Luna switch and used gpt-4.1-mini. They remain labeled accordingly. The voice model/code is unchanged; its contract delegation reaches the same newly configured Luna endpoint.
