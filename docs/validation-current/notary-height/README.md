# Taller desktop Notary transcript

The user asked to use the empty space below the left column for a taller standard speech area. Desktop voice panel now grows into available column height; its caption grid gives the remaining height to THE NOTARY, while controls and text entry keep their size. The scroll region is about 139 logical pixels / 8.3 lines in the default fixture, versus the previous two-line maximum. Mobile layout rules are unchanged.

Local and production `result.json` files record three desktop viewports (1280x800, 1920x1080, 960x600) and one 390x844 touch emulation. Empty and synthetic 1034-character captions have identical desktop height, both action buttons remain in view, and the large bottom gap is gone. Wheel, Home/End, retained reading position and complete Transcript dialog pass. Windows Edge 153 automation; no microphone, physical-phone or actual-model claim.

`layout-regression.json` records the existing seven-size local check with long prose, modal focus/Escape, and ordinary LOCAL RULES Sign. Source build, 34 syntax files and supplied game helper pass. Its canvas-only capture is black behind the HTML UI; the full-page screenshots here provide visual evidence. No paid model call or unit rerun was needed for this CSS-only change. Models remain GPT-Live-1 / GPT-5.6 Luna and prior actual-model evidence remains separately recorded.
