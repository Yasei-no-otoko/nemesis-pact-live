# Compact covenant desktop — 2026-09-14

The user's tall/scrolled desktop contract screenshot prompted a two-column desk: negotiate on the left, review binding benefits/price on the right, with counteroffer and Sign always in view. OpenAI connection/explicit consent now lives in a native dialog; full prose and transcript are separate optional dialogs. Main terms, limitations, revision, provider and preview remain visible. Consent is not automatically granted and mic still requires Start voice.

`result.json` is the local browser matrix; `production/result.json` will identify the deployed matrix. Native Edge 153 on Windows, automated, WebGPU/compute; 1920x1080, 1366x768, 1280x720, 1024x768 and 960x600 desktop viewports. Each desktop case checks no vertical scroll/overflow, visible controls, long caption/contract prose stress, full transcript access, native modal containment, forward/backward Tab wrap, synthetic gamepad focus navigation scoped to the dialog, Escape with proposal preservation and restored focus, explicit Sign and correct right sanctuary. Mobile 390x844 and 320x568 are touch emulation only; vertical scrolling is retained there. No real phones or human operation claim.

Initial keyboard test exposed a native dialog Tab leaving its last control; explicit first/last wrapping fixed it. The retained failure screenshot predates the fix. Initial unit run found two English-locale failures from the Japanese language label; the label now says JAPANESE and 202/202 pass. No assertion was weakened.

Additional regression: 33 JavaScript files syntax checked on Node 22.23.2; supplied develop-web-game Playwright helper passed (canvas-only snapshot cannot show the HTML overlay, so the screenshots here are full-page); five mocked failure cases passed; ordinary LOCAL RULES signing and mid-fight amendment preserved HP/boss/clock. Voice body timeout fixture independently checks native track and peer cleanup. These UI regressions make no paid model calls; previous actual GPT-Live-1 evidence is recorded separately.

The main desktop screen fits the existing 1280x800 logical game canvas scaled to the browser viewport. It does not shrink just this screen to hide overflowing content. Mobile keeps its existing readable single-column layout. Detail dialogs may scroll for exceptionally long transcripts; the primary desktop contract/consent flows do not require scrolling at the tested dimensions.
