# Human recording and native 60fps submission edit

Source: `nemesis-pact-demo (3).webm`, preserved byte-for-byte; hashes and full decode are in `source.json`. It contains one human play session, 119.65 seconds, 1920x1028, VP9/Opus. The source decoded 7022 frames (average 58.688fps), despite a nominal 60fps header. Do not describe every source frame as delivered at 60fps.

The new footage shows a spoken left request, a right/reinforcements correction while GPT-Live-1 displays SPEAKING, the corrected OPENAI / gpt-5.6-luna right contract, Sign, sanctuary combat, spoken sanctuary withdrawal and slower bullets during parley, OPENAI amendment, Sign2, resumed combat and boss defeat. Result: 9836 points, 00:36 combat, 1/1 boss, 2/2 pacts, 4 erased bullets, 870 reflected damage, one amendment. The displayed contract latencies are 2882 / 5957 / 3669ms (n=3, mean 4169.3ms, max 5957ms); these are displayed generation times, not instrumented speech-to-sign latency. The source has desktop Chromium encoder metadata; its exact browser/version/device were not newly confirmed. Earlier Windows Vivaldi and iPhone reports are separate evidence.

DaVinci Resolve Studio 21.1.0.17 performed seven chronological cuts at normal speed with original linked audio. The only FFmpeg preparation was a full-length CFR compatibility transcode. Native Fusion TextPlus supplies English player translations and editorial labels in a separate lower band; the SRT is a sidecar, not an imported subtitle track. No AI dialogue or audio was synthesized for the edit. `edit-plan.json`, `captions.json` and `resolve-final.json` identify every source range, caption, project, timeline and render job. The project export references local media; it does not bundle it.

Final local MP4: `.artifacts/submission/NEMESIS-PACT-human-59s-60fps-en_V1-0001.mp4`. Resolve reports Complete. Full output decode: 3540 frames, 59.0 video seconds, 59.01-second container, 1920x1080, constant 60fps, H.264/AAC, non-silent stereo audio. `export/result.json` records hash/size; exported frames were inspected for readable captions, clauses, signatures and victory. A 60fps export does not recover frames missing from the raw recording.

Audio review remains open: both native analysis and one real gpt-4o-transcribe-diarize request recognized only the player's speech. This alone does not prove the Notary reply is absent; the user has been asked to check it. Original sound is retained. Native automatic Japanese diagnostic subtitles were inaccurate and are not used in the submission. No claim of recorded audible enemy reply is made pending confirmation.

The transcription used one atomic $0.05 reservation in the already approved $3 verification / $10 aggregate OpenAI budget and retained the entire reservation conservatively. A local-only $0.05 recorded-audio allocation was used after the normal direct text cap of zero rejected admission; production settings and aggregate/pool caps were unchanged. No further transcription was retried. See `transcription-run.json` and the shared `../budget-ledger.json`.

This is an actual human gameplay review copy, not synthetic QA. Public viewing URL and source/judge-access decisions remain pending. Historical recordings and failures remain separately labeled and preserved.

Native Windows Edge153.0.4234.32 local-viewer playback reached ended at59.008s,1920x1080, decoded audio bytes present, no media/page errors and zero external requests. This was4x headless playback; dropped presentation frames are expected at that speed and are not a source/game FPS measurement. The first QA helper used wait_for_function and hit the strict viewer CSP; it was corrected to poll the video element without changing CSP. Both attempts are recorded under export/.
