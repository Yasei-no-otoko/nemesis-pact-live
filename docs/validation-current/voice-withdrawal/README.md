# Clause withdrawal regression

The new human `nemesis-pact-demo (2).webm` exposed a frontend intent bug on source 31381f9. During mid-fight parley, `安全は撤回。その代わり敵の弾を遅く` cleared the proposed deal and disabled Sign. The old substring check treated revoking a sanctuary clause as withdrawing the whole proposal.

`isProposalWithdrawal` now recognizes explicit terminal whole-proposal cancellation commands. Clause edits and ambiguous speech continue through the existing validated covenant service. A real cancellation still invalidates the request and disables Sign; accumulated speech is retained so a delayed replacement clause can supersede it. No model, signing, quota or simulation permissions change.

`local/result.json` uses synthetic transcript callbacks in actual Edge controls and the LOCAL RULES path, with zero API calls. It covers clause removal, ordinary whole-proposal cancellation, delayed continuation, two signatures and HP/boss/time continuity. Local rules are bounded offline parsing, not evidence of natural-language model interpretation.

The first production attempt on source `ed48b2895363e511a59a8b84e6da8fce0ab3c3dd`, deployment `dpl_Hg4HZHD3Z5y6Xjf5VMp2UUECpyio`, is preserved in `attempt-1/`. Actual GPT-Live-1 recognized `安全は撤回その代わり敵の弾を遅く` and replied, but emitted no `session.delegation.created` in the 30-second wait. The offer was no longer incorrectly withdrawn, yet the model request did not start. This is a failed end-to-end run, not a pass. One prior Luna text contract had been signed. The actual voice session stopped with confirmed Redis settlement; cost retained $0.05 direct OpenAI and $0.000400 Gateway.

The follow-up adds a bounded application request after quiet meaningful speech when provider delegation is missing. It uses the same generation checks, request cancellation, canonical validation, eight-attempt limit and explicit Sign. Results are returned with the documented session-wide `delegation_id: null`; no provider delegation ID is fabricated. A late delegation for the same input must not generate a second paid contract. The voice prompt also distinguishes clause removal from whole-proposal withdrawal.

Final production regression remains pending. Its separate script allows one Live session per run, synthetic Japanese speech input, a prior text contract/sign, then the recorded withdrawal phrase, a real model amendment and second Sign. It is not human microphone evidence. All failed attempts remain separate.

Attempt 2 on source `3af4ef31e3264dba27f3a8508878c80518514da3` successfully delegated the exact Japanese amendment and displayed a real Luna slow-fire offer, but retained the right sanctuary despite its explicit withdrawal. The assertion failed and no second Sign was clicked. Evidence is preserved in `attempt-2/`; confirmed stop, $0.05 OpenAI and $0.000837 Gateway. The model schema now narrows explicit, complete sanctuary-removal commands to zone `none`, as it already does for explicit left/right corrections, and rejects a retained old zone on decode. Negation, hypothetical discussion, questions and subsequent restoration remain unconstrained or use the newer correction. Other model choices still use the legal rule combinations and explicit Sign.

The subsequent full English flow on source `815d77f` successfully interrupted output audio, corrected right and signed, but Luna omitted requested slower bullets from the affordable slow/charged/weaker-gun amendment. No second Sign was clicked; this stricter regression is preserved in `full-sequence-attempt-1/`. Both actual sessions stopped; $0.10 direct and $0.000823 Gateway retained. The contract prompt was clarified to honor all explicitly requested affordable advantages, including this 2-benefit/2-payment combination. The exact Japanese clause-withdrawal run below remains a separate passing run on 815d77f.


2026-09-14T16:03:51.376030+00:00 — clause withdrawal and delayed delegation fixed in production

- Source `815d77faf465b3447e8a823f7d61777233f860ec`, READY `dpl_5dUHac4Q9pFs6ooP7BD77aBnux1D`, https://nemesis-pact-live.vercel.app. A clause such as "安全は撤回。その代わり敵の弾を遅く" now reaches the existing validated proposal path; explicit whole-proposal withdrawal still invalidates Sign. Quiet meaningful speech can request a proposal when Live emits no delegation, with generation dedupe and the documented session-wide null delegation ID. Eight-attempt limit, old-result cancellation, explicit Sign and all budget caps remain.
- New human 60-second recording proves Luna right correction/Sign/combat/seven erasures, but the prior version misclassified its amendment and no Sign2 is visible. Preserved source SHA256 6b5cba129868cb773212ae52862ce4efd104da6c1d3a4667f6af86b726bf3fa4; the clip is not presented as a successful full-sequence demo. First post-fix automated attempt had no delegation for 30s; failure and confirmed stop are retained.
- Final real-production native Edge 153.0.4234.32 / WEBGPU / COMPUTE / 1280x800 uses Microsoft Haruka Japanese synthetic input, not a new human microphone. One GPT-Live-1 session and 2 real Luna calls passed the exact phrase, no-sanctuary/slow contract, and explicit revisions 1/2. HP 10→10, boss HP retained, time 8.23→8.24s. Connection 2969ms; contract latencies [2311, 2477]ms. All native tracks ended, peers closed, and REST/Redis stop confirmed; zero page errors. Final-run conservative cost OpenAI $0.050000, Gateway $0.000730; failed attempts are additional.
- 219/219 unit tests, 34 Node22 syntax files, build, supplied helper, local cancellation/continuation/sign browser checks and exact production HTML/policy match pass. Ledger 2026-09-14T16:03:49.803Z: direct OpenAI $1.650000/$10; Gateway $0.025993/$20, active 0, kill None. These are conservative application totals, not provider invoices; Vercel grant balance/expiry and infrastructure charges remain unverified.
- Rollback: `vercel rollback dpl_8xZnyqCVV3Dw7jRoXEAxdHaVf5gP --yes`, retaining Redis and the approved budgets. Full-sequence human 60s video, approved public viewing URL and source/judge access remain pending. Code and evidence are committed/pushed; milestone tag follows. Overall GOAL remains incomplete.
