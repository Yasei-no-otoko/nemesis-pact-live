# iPhone human interruption evidence and production correction fix

The user identified the recording device as **iPhone 16 Pro Max**. Browser brand/version was not supplied; WebKit is encoder metadata, not proof of Safari. The original recording shows the earlier tall UI. It is physical human play, not our Windows touch emulation.

`result.json` records source/media hashes, timing, device scope, observed states, separate audio-analysis cost and release metadata. Selected original frames are `12s.png` through `50s.png`. `audio-segments.json` contains estimated speaker segments from the first 40 seconds; ASR time boundaries and words can be imperfect. `transcription-run.json` records one paid batch analysis with gpt-4o-transcribe-diarize. That analysis is not GPT-Live-1 game execution.

At approximately 19.55–20.70 seconds, human and Notary speech interleave while the human changes left to right; around 22 seconds the Notary acknowledges right. The pending old offer cannot be signed. **The resulting signed right contract is LOCAL RULES**, after an upstream unavailable/invalid response. Actual right-side protection subsequently erases four bullets. Parley opens near the end, but a second live negotiation/signature is outside this clip.

## Diagnosis and retained failures

- `model-reproduction-before.json`: two actual Gateway requests; one emitted an unaffordable right + charged reflection + weaker gun contract. This reproduces a rejection class, not the historical reply, which was not retained.
- `model-reproduction-after.json`: three successful real Gateway requests under the first legal-enum fix (v2). These did not establish semantic reliability.
- `production-wrong-direction-before.json` and `.png`: a subsequent real production v2 test returned left after an explicit right correction. Retained as a failed test.
- `production-fix/failure.png`: the same pre-v3 failed run, preserved for provenance. It is not the outcome of the current v3 run.
- `production-fix/result.json` and numbered screenshots: current v3 production success, three real OpenAI responses via Gateway, with ordinary browser controls and real time. The corrected OPENAI contract signs revision 1 and erases three bullets. A no-sanctuary/slow/charged-reflection amendment signs revision 2 while preserving health and elapsed combat time. These are Windows Edge 153 touch emulation with WebGPU, not a second human microphone or physical-iPhone test.

The final server schema enumerates all 36 affordable rule combinations and narrows them for an unambiguous affirmative self-correction. Canonical decoding also rejects a contradictory old zone. The existing public contract validator, explicit Sign, ledger and combat implementation remain authoritative. No extra model retries or cap increases were added. Three production samples do not establish a general success rate or p95.

## Local media

`.artifacts/submission/NEMESIS-PACT-iphone-review-en.mp4` is an English-annotated 50.467-second supplement with original audio/order/speed and honest LOCAL RULES annotation. It does not replace the separate 60-second main video. `playback.json` and `viewing-page-preview.png` record local native Edge playback. Both videos remain local; no public hosting approval has been inferred.

## Subsequent human confirmation

The user then reported “成功。Windows版Vivaldi” for the requested post-fix full voice correction/OPENAI/Sign sequence. `vivaldi-human-confirmation.json` records that explicit report separately from the earlier phone footage. It does not supply a new video, browser version, renderer, latency or identify the iPhone browser.
