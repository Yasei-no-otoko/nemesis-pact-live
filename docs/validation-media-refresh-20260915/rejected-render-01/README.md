# Rejected first native render

The automated decode and original-audio checks passed, but visual inspection found that the saved Fusion captions were absent from the rendered image. This file was never published. The metrics in `result.json` cover only the checks performed there and do not represent editorial acceptance.

The rejected MP4 is retained locally in `.artifacts/media-refresh-20260915/rejected-render-01/`. The final export uses an explicitly activated, refreshed composition for every clip. Caption-margin checks and visual inspection were added before acceptance.
