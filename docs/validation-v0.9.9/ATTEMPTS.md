# Attempt history and evidence scope

Initial Node suite:228/229; the only failure was a0.5.0 report-version assertion. The test now follows the package version; final229/229 passed.

Loop1 desktop long Japanese proposal:4px overflow. Reducing proposal gaps corrected it; final7 layout cases passed. Supplied helper first exported an individual GPU canvas as black; the final helper uses its composited screenshot fallback. Initial images and state outputs remain under loop1.

Initial tiny-phone polish matrix:4/5; REV2 appeared but exact time preservation assertion failed under an active requestAnimationFrame loop. The harness now freezes RAF before navigation, captures step/state/traceback on failures, and fails the process if any case fails. Desktop ability binding checks now call public advanceTime to consume actual click input, rather than injecting the desired action into the simulation helper. This is deterministic UI validation, not ordinary-time performance measurement.

Old covenant browser first failed at a obsolete not-integrated liveVoice string, then because it attempted to edit a hidden mobile Review panel. Updated tests use the actual not-used voice report and visible Negotiate/Review buttons; all18 behavioral checks passed. Model and sign invariants were not relaxed.

Native rendering samples separately use ordinary time and real GPU backends. Canvas2D fallback deliberately disables GPU availability on an offline page; it is an actual fallback rendering/control check, not a physical unsupported device.

Concept generation: proposed direct-key CLI was rejected by automatic approval review before execution, with no detailed reason. A built-in imagegen request succeeded; the exact underlying model and tool cost are not exposed. Direct OpenAI/Gateway ledger was unchanged before release validation. Art brief is adjacent to the concept; no direct-API image call/report is claimed.

Public release helper first violated the viewer CSP by using a string wait predicate. Fixed the helper to a function; production CSP was unchanged. Source download/hash and viewer playback-start passed. One GitHub shell Failed to fetch error is retained separately from the error-free viewer.
