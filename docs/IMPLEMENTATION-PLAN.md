# Shipping plan

GOAL.md is the execution contract and completion checklist. Preserve the framework-free Node 22 build and six-sector campaign.

1. Record input SHA256, compare upstream, preserve baseline and run current tests.
2. Establish a dedicated Vercel project and secure application session / durable atomic budget gate. Pass real text inference through the canonical validator, explicit signature and deterministic combat.
3. Integrate the verified official GPT-Live-1 protocol into that same proposal path. Keep voice interruptions separate from request cancellation. Enforce session duration upstream.
4. Verify stale responses, corrections, withdrawal, duplicate events, concurrency, quota outages, reconnect/cleanup and non-resetting amendments.
5. Improve readability, first-contact flow, mobile interaction and voice audio ducking; run browser regressions.
6. Verify production and real mic behavior, capture actual 60-second footage, publish through an authorized destination and finalize six English submission fields.

No blanket live completion claims: every gate requires observed evidence. Vercel target <= $20, suppression at $25 and $5 buffer; actual credits are unverified. OpenAI is separately approved up to $10 total: verification $3, public $5, judging $2. The production text vertical slice is verified; human Live interruption and final demo gates remain open. See WORKLOG.md for current commit and measurements. All paid API routes must fail closed until a verified budget and durable store exist.

Current production: `3ff6fb06fd212cc50614a4b6dd67e52d5325bce6` / `dpl_9VterKz8JkDeyjVGpdJrVqcm3e48`. First human voice contract change and boss defeat are user-reported; interruption and voice parley remain open. Recorder repair passed an actual 60-second browser capture fixture. Prepared six English application fields and current licenses. Waiting on the human retry in the prepared Chrome tab; source publication approval is pending. Current regression has 198 passed unit tests, an actual isolated Redis handler/signature race test, and ~60fps short hardware-rendering samples. Post-deploy LOCAL RULES browser correction and amendment passed without API calls. Voice startup now times out through a stalled body; native browser track/peer cleanup and late-session handling passed an explicit response fixture. See RELEASE-CANDIDATE.json for exact build hashes.
