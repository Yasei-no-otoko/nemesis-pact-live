# NEMESIS PACT v0.9.9 — craft pass

Requested 2026-09-15 JST; submission deadline 23:59 JST. Continue the verified v0.5.0 lineage, not a reset to the original ZIP.

Baseline: clean `ship/living-covenant`, HEAD `70e095878078d6808e5a02117b78099312394e80`; production application `cd333643ba038bfe97b49ac3f641c563520d7f61`, deployment `dpl_D8wY2pL3CSWN7tzg3R9qzKTXf91o`. No project CLAUDE.md/AGENTS.md found at root or the two enclosing directories; session AGENTS instructions apply. Preserve the original GOAL.md and all successful human video evidence.

## Improvement loop

1. Establish current tests and screenshots; generate one bounded imagegen concept sheet.
2. Translate the concept into live procedural graphics and accessible UI: a distinctive Notary identity, restrained brass/ink surfaces, readable combat silhouettes, clear interactive states and a coherent first-contact flow.
3. Play and inspect desktop, compact desktop and phone emulation; correct layout, contrast and interaction regressions. Preserve working Live/Luna, explicit Sign, quotas, recording, campaign and offline builds.
4. Run regression and actual production verification; commit/push and readable milestone tags; release v0.9.9 to the existing Vercel project. Record exact scope, costs, limitations and rollback.

Baseline Node 22.23.2: 229/229 tests, 23812.7683 ms; 34 syntax files; build passed. Hosted 382304 bytes, offline 382254 bytes. No new API calls for baseline tests.

Budget read 2026-09-14T18:21:40.881Z: direct OpenAI $2.15/$10, verification $0.90/$3; Gateway $0.031421/$20; active 0, kill null. Conservative application accounting, not provider invoices. The proposed direct-key image CLI was rejected before execution by automatic approval review. One built-in imagegen call completed without reading a key; its underlying model/cost is not exposed. No direct-API image reservation or call was made. No public model settings or budget expansion.

The submitted 59-second/60fps human iPhone movie remains evidence of the earlier verified application. Do not relabel it as footage of v0.9.9; a refreshed clip is optional if it requires new human recording.

## Implemented craft loops

The concept is `output/imagegen/nemesis-v099-concept.png`; the adjacent prompt text is the art brief, not a CLI execution record. Generation used the built-in imagegen tool. Fictional mechanics in the concept are not game features. Its palette, geometry and typography were translated into local CSS/procedural meshes, retaining a self-contained offline HTML. No bitmap or additional runtime dependency is loaded by the game.

- Loop 1: title typography and primary flow; ceremonial six-wing Notary; thinner brass architecture; two-column contract desk; mobile Negotiate/Review steps; keyboard/click ability controls; actual boss HP percentage; signed chord; sanctuary annotations tied to the actual zone radius.
- Loop 2: inspect native Edge screenshots. Correct broad torus geometry, obscured diamond core, 320px header/control overflow, contract receipt over the portrait arena, and 4px long-Japanese desktop overflow. Keep only the visible mobile panel in controller focus traversal.
- Loop 3: native thin-ring meshes, clear diamond, title ring placement around the enemy, smaller inactive local voice panel. Desktop5 + phone-emulation2 contract layout cases passed. 229/229 Node22 tests and34 syntax files passed, 23821.904ms. Twelve deterministic campaign cases all defeated six bosses; First Contact simulations remain separate from ordinary human play.

The supplied develop-web-game helper was executed. A local compatibility adapter selects installed native Edge and the supplied screenshot fallback because exporting only the GPU canvas was black. The final composited capture contains the actual rendered game and overlay; no game drawing or simulation was substituted. Browser evidence distinguishes WebGPU, WebGL2 software/hardware, controlled time, ordinary time and emulated phone sizes.

Release and production integration evidence will be recorded in V0.9.9-VALIDATION.md and RELEASE-CANDIDATE.json.

Completed: production/live regression and public versioned source release passed. See V0.9.9-VALIDATION.md for exact evidence and limitations.
