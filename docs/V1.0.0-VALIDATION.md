# NEMESIS PACT v1.0.0 — encounter finish

Work in progress until the production and release entries below are filled. Current verified predecessor: v0.9.9 application66e6641, checkout19b9ba3, deployment dpl_CsUJ386UFWLPKg4UN7r1jvCpcuoB.

The new concept is `output/imagegen/nemesis-v100-concept.png`; its exact prompt is adjacent. A generated combat/result board informed the engraved seal, restrained victory palette and actual signed receipt. Example numbers in the concept are not game evidence. Runtime visuals use local CSS/geometry; no image request, framework or new dependency was added.

First Contact now has dedicated honored / unbound / lost endings. The final canonical terms, sanctuary interceptions, reflected hit damage, signed revisions, reflected bullets, elapsed time and hull come from the actual run. Totals include earlier revisions; void contracts are labeled. Retry retains seed and Notary memory. Full campaign opens flight command. Actual laser tells and Nova readiness drive compact guidance, while pause/help explain amendment limits and automatic fire. The initial signature card is smaller and lasts1.6 seconds instead of2.5.

## Local verification

- Baseline229/229. Final229/229 Node22.23.2,23837.0142ms;34 syntax files and standalone/hosted build.
- First unit attempt found CRLF conversion in the unchanged core fingerprint. Restored original LF; the canonical mechanism hash then passed. No physics change was required.
- First Contact12/12 and six-boss campaign12/12 legal-input deterministic simulations won. Full-state automated pilots are not human difficulty evidence.
- Offline browser contract suite18 assertions passed at1280x800,390x844,320x568; zero external calls. Desktop/mobile polish5/5 passed, including actual action buttons, tabs and modal controls. A stale test origin allowlist was corrected from8099 to the configured URL after it incorrectly classified the local navigation as external.
- `loop1/` contains actual native Edge captures, inspected alongside baseline; `loop1/helper/` contains the supplied game skill helper and state. The helper uses an installed Playwright/Edge adapter and native composited screenshot fallback for layered GPU/canvas surfaces. It reported WEBGL2/PBR. Frozen-RAF captures and explicit simulation time are not performance samples.
- Dedicated ending, two-signature continuity, retry and contextual-help matrix passed at1280x800,390x844,320x568. All six raw result metrics match the run, canonical clauses match the signed spec, and memory survives retry without double saving. Honored wins, legal breach wins and deliberate contact losses use permitted actions with a deterministic clock. No HP/time/phase overrides.

The screenshot loop exposed a LOCAL RULES gap: “Remove the sanctuary” was counteroffered with a center sanctuary. The authored keyword fallback now accepts explicit remove/drop/without phrases. Final regression requires sanctuary removal plus charged reflection/weaker gun. Canonical validation and paid model paths are unchanged. Draft harness syntax/runtime mistakes were corrected; final browser report passes all three sizes.

## Production / publication

Pending at this local checkpoint. The existing human59-second1080p60 video remains unchanged at https://www.circle-hydrangea.net/nemesis-pact-live/ . It shows an earlier verified application, not v1.0.0 artwork or a new physical-device test.

## Costs and limits

Pre-regression shared ledger2026-09-15T00:17:04.549Z: directOpenAI$2.25/$10, Vercel AI Gateway$0.032718/$20, active0, killnull. No caps or paid settings changed. These are conservative app reservations, not provider invoices. Exact key-specific grant balance/expiry, infrastructure charges and built-in imagegen-tool cost remain unverified. Do not infer an unconditional30USD total guarantee. Phone sizes in new browser matrices are Windows emulation; earlier iPhone16ProMax/Safari and Vivaldi human evidence remains separately identified.

Rollback the v1.0.0 application to the verified predecessor with `vercel rollback dpl_CsUJ386UFWLPKg4UN7r1jvCpcuoB --yes`. Preserve Redis, secrets, quotas, repository history and human media.
