# Delivery v0.5.0

Primary playable artifact: `dist/NEMESIS-PACT.html` (offline, explicit local rules). Hosted entry: `public/index.html`; server: `tools/serve.js`. The current candidate is available at `https://nemesis-pact-live.vercel.app`; source branch, commit, deployment and build hashes are recorded in `RELEASE-CANDIDATE.json`. Current overview is `README.md` / `README-JA.md`; read `VALIDATION-CURRENT.md`, not older baseline reports.

The candidate includes production text contract generation through Vercel AI Gateway, GPT-Live-1 production transport evidence, explicit signing and deterministic combat, plus the explicit LOCAL RULES fallback. The current GPT-Live-1 → GPT-5.6 Luna path passed actual production interruption, corrected right Sign, combat and spoken amendment/Sign in automated Edge with synthetic audio. It is combined real-model evidence, not a new human microphone recording. It does not include an API key or a submitted contest form. The 60-second human English-captioned MP4 is saved and playback-verified locally, but its public viewing URL is pending approval/hosting and the main clip does not show interruption during the Notary's reply. A separate 50.47-second iPhone clip shows interruption but signs the corrected proposal through LOCAL RULES, so a single 60-second all-mechanics demonstration remains outstanding. The repository is private pending a source-publication or judge-access decision.

The supplied v0.5.0 source ZIP is the baseline. Human Chrome footage and a separate iPhone 16 Pro Max clip are recorded in the validation evidence; the phone clip's corrected signed contract is explicitly LOCAL RULES, and browser/version, latest compact UI and FPS on physical phones, Android and macOS Safari remain limited or unverified. Version numbers, hashes and source differences document this revision; they do not independently establish legal ownership or all historical development dates.
