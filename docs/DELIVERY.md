# Delivery v0.5.0

Primary playable artifact: `dist/NEMESIS-PACT.html` (offline, explicit local rules). Hosted entry: `public/index.html`; server: `tools/serve.js`. Current overview is `README.md` / `README-JA.md`; read the current validation report, not older baseline reports.

This delivery does not include an API key, configured live endpoint, live voice transport, Vercel deployment, public video URL or submitted contest form. Submission-pack fields are drafts; missing representative email and final team name are not inferred from old metadata.

The 0.4.1 source ZIP supplied by the user is the baseline. Version numbers, hashes and source differences document this revision; they do not independently establish legal ownership or all historical development dates.
