const test=require('node:test');const assert=require('node:assert/strict');const DemoRecorder=require('../src/demo.js').DemoRecorder;
test('demo recorder is disabled without explicit demo query',()=>{const d=new DemoRecorder({document:{body:{append(){throw Error('must not attach');}}}});assert.equal(d.attach(),false);});
test('state timer uses 60 seconds and cleanup stops display tracks',async()=>{let timer,stopped=0;const video={readyState:'live',addEventListener(){},stop(){stopped++}};const display={getVideoTracks:()=>[video],getAudioTracks:()=>[],getTracks:()=>[video]};let start;const nav={mediaDevices:{getDisplayMedia:async opts=>{start=opts;return display;}}};class MS{constructor(tracks){this.tracks=tracks}getAudioTracks(){return this.tracks.filter(t=>t.kind==='audio')}getVideoTracks(){return this.tracks.filter(t=>t.kind==='video')}getTracks(){return this.tracks}}class MR{static isTypeSupported(){return false}constructor(){this.state='inactive';this.mimeType='video/webm';this.listeners={}}addEventListener(k,f){this.listeners[k]=f}start(){this.state='recording'}stop(){this.state='inactive';this.listeners.stop?.()}};const d=new DemoRecorder({navigator:nav,MediaRecorder:MR,MediaStream:MS,document:{createElement:()=>({addEventListener(){},click(){}}),body:{append(){}},defaultView:{addEventListener(){}}},setTimeout:(f,ms)=>{timer={f,ms};return 1},clearTimeout:()=>{},setInterval:()=>2,clearInterval:()=>{}});d.attach=()=>true;assert.equal(await d.start(),true);assert.equal(start.video.frameRate,30);assert.equal(timer.ms,60000);assert.equal(d.stop('user'),true);assert.equal(stopped,1);});
test('tab audio and microphone are mixed, output is added only without tab audio',async()=>{const connected=[];class Source{constructor(stream){this.stream=stream}connect(dest){connected.push([this.stream,dest])}}class AC{createMediaStreamDestination(){return {stream:{getAudioTracks:()=>[{kind:'audio',readyState:'live'}]}}}createMediaStreamSource(s){return new Source(s)}close(){}}const track=k=>({kind:k,readyState:'live',stop(){}}),video=track('video'),tab=track('audio'),mic={getAudioTracks:()=>[track('audio')]},enemy={getAudioTracks:()=>[track('audio')]};const d=new DemoRecorder({AudioContext:AC,MediaStream:class{constructor(t){this.t=t}},navigator:{mediaDevices:{getDisplayMedia:async()=>({getVideoTracks:()=>[video],getAudioTracks:()=>[tab],getTracks:()=>[video,tab]})}},MediaRecorder:class{},document:{},setTimeout,clearTimeout,setInterval,clearInterval});d.display={getVideoTracks:()=>[video],getAudioTracks:()=>[tab]};d.attachVoice(mic,'input');d.attachVoice(enemy,'output');const mixed=d._buildAudio(0);assert.ok(mixed);assert.equal(connected.length,2);assert.equal(connected[0][0],d.display);assert.equal(connected[1][0],mic);});
test('attachVoice does not stop or clone game-owned tracks',()=>{let stopped=0;const track={stop:()=>stopped++};const stream={getAudioTracks:()=>[track]};const d=new DemoRecorder();assert.equal(d.attachVoice(stream,'input'),true);assert.equal(d.voice.input,stream);assert.equal(stopped,0);});
