# GOAL — NEMESIS PACT: Ship the Living Covenant

NEMESIS PACT v0.5.0を正しい基点として、ゲームの完成度を高め、実際のGPT-Live-1による音声交渉を実装し、Vercelの本番HTTPS URLに公開する。調査・設計・モック・ローカル動作だけで終わらず、実装、回帰テスト、実API接続、本番ブラウザ検証、提出用デモ素材まで完遂する。

審査員が「話しかける → 敵の返答に割り込んで条件を変える → 契約を確認して署名する → 実際の弾幕と戦い方が変わる → 再交渉する」を体験できることが中心目標。AI音声の追加だけでなく、プレイヤーの言葉が戦闘のルールを変えるゲームとして仕上げる。受賞は保証できる成果物ではないため、検証可能な体験と実装を完了条件にする。

## 1. 固定条件と開始時の事実確認

- 参加トラック：AI-Native Game Prototype。
- 審査配点：Meaningful use of OpenAI tools 30%、Originality 25%、Playability / Utility 25%、Execution and craft 20%。
- 正式制作期間：2026-09-11 20:00〜2026-09-15 23:59、Asia/Tokyo。提出期限は2026-09-15 23:59 JST。9月16日は選考結果の通知、最終審査は9月17日。提出リンクは少なくとも9月17日終了まで閲覧可能に保つ。
- 1分デモ動画URLが提出必須。動作デモとコードリポジトリのURLも準備する。フォーム送信、法的同意、チーム情報の変更は本人の確認なく実行しない。
- 英語UIを維持する。要求文と音声は英語・日本語に対応し、マイクを使わなくても同じ契約経路で遊べるようにする。
- 既存候補リポジトリ：Yasei-no-otoko/openai-100-hour-game-builder-challenge。対象候補：NEMESIS-PACT-3D/。
- 2026-09-14のGitHub読取では、そのmain内のpackage.jsonは0.3.6.1だった。mainを0.5.0と決めつけず、開始時に再確認する。
- 正本となる入力：NEMESIS-PACT-3D-EN-v0.5.0-source.zip。アーカイブ内のルートはNEMESIS-PACT-v0.5.0/。必要に応じてNEMESIS-PACT-Submission-Pack-v0.5.0.zipも読む。
- ZIPはこの作業のワークスペースで実在を確認する。別のChatGPT環境の /mnt/data がCodexにも存在するとは仮定しない。見つからない場合は入力不足だけを具体的に報告する。
- Vercelクレジットはユーザー申告で30米ドル。2026-09-14の接続済みアカウント読取では、wildmans-projectsはHobbyで、確認できた既存プロジェクトは無関係なsake-ecのみ。実行時に再確認し、そのプロジェクトを変更しない。
- 30ドルの残高・有効期限・適用範囲は未検証。Vercelインフラ用、AI Gateway用、v0用などを混同しない。OpenAIへ直接支払うAPI利用料は別管理とする。

## 2. 優先順位

P0-A：v0.5.0の正確な取り込み、実OpenAIモデルによるテキスト交渉、Vercel上のプレイ可能な縦切り実装。
P0-B：本物のGPT-Live-1による音声交渉・割り込み・言い直し・再交渉。これは完遂に必須であり、後回しの構想や任意のおまけにしない。
P0-C：契約の正確性、費用制限、秘密保護、マイク拒否・通信障害でも遊べること。
P1：視認性、操作感、導線、音響、スマートフォン品質、60秒デモと応募素材。

まず小さな本番経路を通し、その上で品質を上げる。全面的なエンジン置換、理由のないReact/Next.js移行、マルチプレイ、課金、巨大な追加コンテンツは対象外。既存の6セクターキャンペーンは保持する。

## 3. 最初に実行する監査と基点の確保

1. AGENTS.md等の作業指示、git status、remote、branch、未コミット差分を確認する。ユーザーの変更を消さない。必要なら専用ブランチまたはworktreeで作業する。
2. ZIPと現行ツリーを比較し、v0.5.0のコードを対象フォルダへ統合する。より新しいユーザー差分は保持する。古いコードへ巻き戻さない。ZIPのハッシュと取り込み差分を記録する。
3. 次を読む：README.md、README-JA.md、package.json、vercel.json、.env.example、tools/build.js、tools/serve.js、src/covenant.js、src/game.js、src/intelligence.js、server/covenant.mjs、server/intelligence.mjs、api/、docs/LIVING-COVENANT.md、docs/VALIDATION-0.5.0.md、docs/GPT-LIVE-1-ASSESSMENT-JA.md。
4. 初期状態で npm run check、npm test、npm run build を実行する。旧報告の164件成功を今回の成功証拠として流用しない。現在の件数・失敗・環境を保存する。
5. 初期実装はNode 22、フレームワークなし、npm run build、出力public。ビルドはindex.htmlとsrc/を束ね、public/index.htmlとdist/NEMESIS-PACT.htmlを生成する。生成HTMLだけを修正しない。
6. 現状のvercel.jsonは Permissions-Policy: microphone=() でマイク禁止。Hosted版CSPはconnect-src 'self'、単体HTMLはconnect-src 'none'。音声接続に必要な修正をビルド元から行い、ヘッダーとmeta CSPの両方を本番レスポンスで検証する。
7. api/covenant.mjsとapi/intelligence.mjsは現在maxDuration 15秒。音声やsidebandを同じ制限のまま流用しない。採用ランタイム・プラン・上流タイムアウトに整合させる。
8. 既存のPILOT_ACCESS_TOKENとプロセス内回数制限は公開サービスの認証・分散クォータ・課金上限ではない。新旧両APIと音声経路を共通の費用・アクセス管理下に置く。

## 4. 仕上げるゲーム体験

- First Contactを主導線にする。タイトルで「Negotiate the rules. Fight your deal.」が伝わり、説明を読まなくても操作と次の行動が分かる。
- 1プレイは概ね2〜3分を目標とする。初回交渉、戦闘、1回の再交渉、決着と結果を短い体験にまとめる。これは新しい目標値であり、計測して調整する。
- 既存の結界位置、敵弾速度、反射強化と代償を維持・改善し、提示された利益と代償がそのまま戦闘に現れるようにする。
- 「左を安全にして」「右に変えて、増援は構わない」「弾を遅くする代わりに通常射撃を弱くして」などが同じスキーマへ正規化されることを確認する。
- 契約前後の変更点を視覚化する。現在のrevision、結界が実際に消した敵弾数、反射で実際に与えたダメージを表示する。架空の効果カウンターを作らない。
- 提案を生成しただけでは戦闘ルールを変更しない。Signボタンへの明示操作のみで適用する。音声の「はい」やモデルのツール呼び出しだけで署名しない。
- 再交渉ではHP、ボスHP、経過時間、既存の戦闘成果を保持する。キャンセルは元の契約へ戻す。破棄・違反の代償も明確にする。
- 黒画面の再発を防ぎ、機体・敵弾・床・レーザー・結界の識別を優先する。結界が防がない攻撃も見分けられるようにする。
- モバイルのタッチ領域、セーフエリア、ソフトキーボード、画面回転、スクロール干渉を調整する。過剰なブルーム、振動、フラッシュを避け、低負荷設定とreduced motionを用意する。
- 音声中はBGMを下げ、字幕・消音・音量調整を使えるようにする。既存キャンペーンとオフライン版を壊さない。

## 5. GPT-Live-1の実装要件

### 5.1 公式仕様の確認と実アクセス

GPT-Live-1は2026-09-10にAPI提供が発表されている[S1,S2]。ただし、モデルの公開と使用するプロジェクトのアクセス権は別。実行時の公式Live APIガイド、SDK、モデルカタログ、実アカウントで確認する。

モデル名だけを既存Realtimeサンプルへ置換しない。セッション作成、認証、WebRTC/音声形式、delegation、字幕、終了、キャンセルのイベント名・ペイロードを一次資料から確認し、SDKバージョンと参照URL・確認日をdocsへ残す。記事に省略されたハンドシェイクを捏造しない。SIP用のacceptや保存済みセッションのforkを、ブラウザの初回接続APIとして流用しない。

OPENAI_MODELは契約を生成するResponses対応テキストモデルとして残す。音声モデル設定は別に設ける。音声層をgpt-live-1、契約層を利用可能で十分に速く、厳密な構造化出力に対応するモデルとし、実測して選ぶ。

GPT-Live-1が実際に使用できない場合は、HTTPステータス等の秘匿化した証拠、試した正式な経路、必要なアカウント操作を記録する。別モデル、ブラウザTTS、録音音声、STT→LLM→TTSをGPT-Live-1実装済みと表示しない。テキスト実装等の独立作業は継続するが、音声の未達を隠して完了にしない。

### 5.2 音声とルールの役割分離

望ましい経路：
Browser microphone → GPT-Live-1 → delegation → bounded covenant service → canonical proposal → explicit signature → deterministic combat

ブラウザと音声サービスのWebRTC直結を第一候補とする。Vercelは安全なセッション初期化、認可、契約生成、費用制限を担当する。実際のLive APIの正式な接続方式に従い、長期APIキーをブラウザへ渡さない。

managed delegationかclient delegationかを、既存の契約サービスの再利用、検証可能性、費用制限、権限境界で選ぶ。既存サービスを安全に再利用できるならclient delegationを優先するが、未確認のプロトコルに固執しない。

必要なWebSocket/sidebandはVercel Fluid computeを候補として実デプロイで確認する。Vercelは2026-06-22にWebSocketのPublic Betaを告知している[S3]ため「Vercelでは絶対にできない」と決めつけない。一方、接続はFunctionの最大実行時間に制約され、再接続先は同じインスタンスとは限らない[S4]。無料枠・プラン・終了処理・分散状態を確認する。単純な再接続で会話と署名が自動的に復旧すると仮定しない。

音声の常時中継、別クラウドの常駐サーバー、追加有料サービスは初期案にしない。必要性が実証された場合だけ代替案と費用を比較し、追加契約は承認を得る。

### 5.3 会話と署名の整合性

- 初回契約と再交渉の画面でのみ、明示的な音声開始操作によりマイクを使う。初期値はOFF。
- 敵は短く人格的に応答し、要求を理解して取引を提案する。「AIとして」等の説明や長い独白を避ける。対応できない要求は、実装済みの条件で対案を示す。
- フロント音声の指示と契約生成用バックエンドの指示を分離する。ツールは提案作成・取消・状態照会に限定し、任意コード実行やゲーム状態の直接変更を許さない。
- runId、sessionId、requestId、delegationId、proposalId、revision等を必要な範囲で関連付ける。API由来IDとアプリ内IDを区別する。
- 「左、いや右」「前の提案を撤回」「今の条件で」といった訂正、重複イベント、順序逆転、再接続で、古い提案が署名されないようにする。
- 音声への割り込みとバックエンド処理の取消は別物[S2]。無関係な相づちでは処理を壊さず、条件の訂正・撤回時には古い要求を無効化する。取消が上流に間に合わなくても、遅延結果は適用しない。
- 割り込み中・要求更新中は古い提案のSignを無効にする。表示された正規化済み条件と署名対象を結び、TOCTOUと二重署名を防ぐ。
- モデルの口頭説明は権威あるルールではない。正規化済み条件を会話へ返し、読み上げと画面を一致させる。表示外の無敵や体力回復を口約束させない。
- 戦闘の描画・移動・当たり判定・弾幕をLLMの応答待ちにしない。

### 5.4 音声UIと終了処理

接続中、聴取中、応答中、再接続中、停止、エラーを識別できるUI、字幕、ミュート、終了ボタンを実装する。マイク拒否、端末にマイクなし、音声出力の自動再生制限、切断時は即座にテキストへ戻れるようにする。

署名、キャンセル、タイムアウト、結果画面、ページ離脱・バックグラウンド化で音声を終了する。MediaStream tracks、PeerConnection、DataChannel、イベントリスナー、タイマー、サーバー側セッションを処理する。再接続でセッションを増殖させない。

接続切断・権限・durationはブラウザだけでなくサーバーまたはプロバイダー側でも強制する。必要なら公式hangup操作[S6]を使用する。短命な初期認証情報の失効が、確立済み音声接続の終了と同じ意味だと仮定しない。

## 6. 公開API・セキュリティ・予算

### 6.1 Vercelの30ドル

- 追加のVercel消費は30ドル以内を要件とする。運用設計値は目標20ドル以下、25ドルで追加消費を抑制し、残り5ドルを余裕として確保する。これは価格保証ではない。
- 開始時にクレジット種別、残高、有効期限、現在の請求期間、他プロジェクト消費、プランで使える機能と許容用途を確認する。確認不能な部分は未確認と記す。
- 有料プランへの変更、支払方法の追加、自動チャージ、ドメイン購入、有料DB・Marketplace契約、無料トライアルからの自動移行は独断で行わない。
- Spend Managementの実際の対象と閾値を確認する。公開説明ではPro向けで、クレジット等を超えるチーム全体の従量消費が対象となり、追加サービス等は対象外[S5]。クレジット30ドルだから閾値を30にすれば安全、とは扱わない。
- チーム全体の停止は他案件を巻き込むため実行しない。まず本プロジェクトの有料AI機能を制限し、可能な範囲で静的な試遊と動画を残す。インフラ消費も危険な場合は公開継続と予算の両立不能を報告し、予算上限を優先する。
- プレビュー・再デプロイを無制限に反復しない。診断用動画、WAV、旧検証画像、テスト資材は本番配信から除外する。使用量を節目で確認し、推定と請求確定値を区別する。投入用ZIPも公開ディレクトリや不要な本番バンドルに混ぜない。

### 6.2 OpenAIの別予算

- VercelクレジットをOpenAI直接APIの残高とみなさない。GPT-Live-1音声層は発表時0.05ドル/分、バックエンドとツールは別料金[S1,S2]。実行時の単価で再計算する。
- 既存の承認済みOpenAI予算を使う。未指定なら、検証用には確認できた利用可能な無償クレジットから最大3ドルを設計上限とし、それ以外の有料消費や公開運用枠は必要な承認を求める。新規購入や自動チャージは行わない。
- 公開運用の合計予算、検証枠、審査用の予約枠を分ける。合計予算が設定されていない状態で無制限の公開live APIを有効にしない。
- 初期の音声制限目標：交渉1回45秒まで、1プレイ累計90秒まで、同一セッションの重複接続なし。実プロトコル・プラン制約と整合する範囲で短縮可能とする。
- テキスト入力長、モデル入力・出力トークン、delegation回数、リトライ回数、同時実行数に上限を設ける。クライアントからモデル、ツール、上限を自由指定させない。
- 消費前に最大見込み費用を原子的に予約し、使用量取得後に精算する。タイムアウトや切断でも上流課金が止まったとは決めつけず、不明な消費をゼロへ戻さない。
- 音声秒数とテキスト使用量を別計測する。ブラウザのタイマー、localStorage、単一Functionの変数だけを支出上限の根拠にしない。

### 6.3 公開制御

既存共有pilot tokenの公開配布を廃止し、署名付きの短命なアプリセッション、Origin検証、必要なCSRF対策、IP等による補助制限、永続的・原子的な共有クォータ、全体予算、kill switchを組み合わせる。匿名セッションだけでは本人性・多重取得防止を保証できないことを踏まえる。

既存または承認可能な無料枠の永続ストアを最小限利用する。新しい有料契約は作らない。無料で成立しなければ、限定された審査アクセス等の安全な形を実装し、不特定多数向けlive公開の未解決事項を明記する。クォータストア障害時はlive呼び出しをfail closedにし、ローカル試遊は継続可能にする。

旧/api/intelligenceを迂回路にしない。セッション作成、契約生成、音声delegation、再接続、停止APIを認可対象とし、他人のsessionIdを操作できないようにする。

OPENAI_API_KEY等の秘密値は、既存の承認済み設定または安全なOpenAI Platform/Codexキー設定フローで扱う。チャットへの貼り付けを求めず、Git・静的HTML・公開環境変数・ログ・スクリーンショット・HAR・録画へ出さない。秘密がなければ名称と用途だけを報告する。

Hosted版はmicrophone=(self)等の必要最小限の権限へ変更する。CSPは選んだ公式接続先だけを許可し、connect-src *やCSPの丸ごと無効化を避ける。offline版の通信禁止は維持する。公開サイトのヘッダーだけでなく、iframeで動かす場合のマイク許可も検証する。

音声の外部送信とAI生成音声であることを開始前に示す。プレイヤー音声や全文会話を標準で永続保存しない。記録用の同意とデモ録画を通常のプレイから分離する。保存OFFはプロバイダー側の全保持がゼロという保証ではないため、実際の設定に合った説明にする。

## 7. 実行する検証

既存の npm run check / npm test / npm run build / npm run test:covenant / npm run test:covenant-browser / npm run test:covenant-simulation / npm run test:campaign / npm run test:server を確認し、変更に対応したテストを追加する。利用可能な実ブラウザで検証し、同じテストを根拠なく繰り返さない。

最低限のケース：
1. 実テキスト推論→有効な契約→明示署名→戦闘効果。
2. 実GPT-Live-1接続→発話→delegation→同じ契約経路への到達。
3. 敵の発話中の「左、いや右」への訂正が新しい提案に反映される。
4. 発話の撤回、古い返答の遅着、同じイベントの二重到着、署名連打でも誤適用がない。
5. 再交渉で契約だけ変わり、HP・ボスHP・経過時間が維持される。
6. 不正JSON、未定義条件、予算外の強化、プロンプトインジェクションを拒否する。
7. マイク拒否・音声出力拒否・切断・429・上流timeout・クォータストア障害・予算到達から戻れる。
8. 再起動、別Functionインスタンス、並列リクエストでもクォータを突破しない。
9. 停止後の音声接続・マイクtrack・再接続timerが残らず、上流セッション終了も確認できる。
10. productionをログアウト相当のブラウザで開き、タイトル→交渉→戦闘→結果→再試行を完走できる。

1280×800、390×844、320×568程度を確認する。Chrome/EdgeのWebGL2を基準に、利用可能なWebGPU、macOS Safari、iPhone/Android実機を追加する。デスクトップ60fps、モバイル30fps以上を目標にフレーム時間・起動時間を測る。実機がない場合はエミュレーションを実機と表現せず、未検証ゲートに残す。

モックは自動テストに使用してよいが、ライブ成功の証拠とは分ける。実際の音声入力による統合試験、発話に割り込む試験、通常の人間操作による試遊を区別する。合成音声やfake mediaを使った場合はその条件を記録する。

音声接続、最初の応答、契約確定の時間を分けて計測する。少数サンプルから安定したp95や一般的な成功率を主張しない。平均・最大・件数・環境を記録し、十分な件数がある場合だけ分位値を示す。

## 8. Vercel本番デプロイと提出物

- 接続済みVercel/MCP/CLIで対象アカウントを確認する。該当プロジェクトがあれば再利用し、なければNEMESIS PACT用を新規作成する。無関係な案件に紐付けない。
- モノレポならRoot Directoryを実際のNEMESIS-PACT-3D/等へ設定する。Build Command、Output Directory、Node版、.vercelignore、API関数のバンドルとroutingを確認する。
- 最小依存を採用してロックファイルを保存する。既存ビルドを保ち、音声のためだけに全画面を作り直さない。
- previewでAPIと音声を試験後、productionへ反映する。PreviewとProductionの秘密設定・許可Origin・認証を分離し、必要なら再デプロイする。
- デプロイREADYだけで成功にしない。本番HTTPS上のマイク取得、API、音声、ゲーム、ブラウザconsole、CSP、サーバーログを確認する。
- プレビューの認証付き一時URLを審査用の恒久URLとして提出しない。審査員が通れるアクセス方法を実際に確認する。共有コードを使う場合は限定した審査アクセスに留める。
- runtime表示は音声providerと契約providerを分ける。例：VOICE: GPT-LIVE-1 / CONTRACT: OPENAI または LOCAL RULES。片方の成功で両方成功に見せない。
- 9月15日の期限前に提出候補のcommit、release tag、deployment ID、ビルドハッシュを確定する。日時を偽装しない。締切後の成果を期間内の成果として混ぜず、更新は主催者の許容範囲を確認する。
- 60秒の英語字幕付き動画を実際の動作から制作する。音声の言い直し、提案変更、署名、結界や弾速の変化、再交渉を見せる。未実行のAI応答を後付けしてライブ動作に見せない。
- 動画ファイルを保存し、利用可能な承認済み公開先へ掲載してURLを検証する。大容量動画をVercelの30ドル枠へ安易に載せない。公開先がなければファイルと残作業を明記する。
- プロジェクト説明、4審査基準、既存コード・外部素材・ライセンス開示の計6欄をそれぞれ英語200語以内で更新する。実装済み、実証済み、未検証を区別する。
- ライセンスを新たに勝手に付与しない。素材・依存・AI利用・既存コードの由来と制作期間を記録する。

## 9. 作業の進め方

短い計画の後、実装へ進む。調査だけ、設計書だけ、TODOだけで終了しない。コード、テスト、ライブ縦切り、本番公開の順で成果を積み上げる。

docs/IMPLEMENTATION-PLAN.mdとdocs/WORKLOG.mdに、現在のcommit、完了した検証、残タスク、重要な判断、失敗の根拠、予算消費を記録する。再開時はこれを読み、既に解決した調査を繰り返さない。

並列エージェントが使える場合は、音声/API、ゲーム/UX、QA/デプロイに責任を分ける。同じファイルの競合を避け、最後は単一の統合経路で検証する。並列化や再試行も予算内で行う。

普通の設計判断は自分で行う。認証、課金承認、正式なAPIアクセス、実マイク操作、公開先の権限など人にしか解決できない阻害要因だけ、必要な操作を短く依頼する。その間も独立タスクは進める。承認待ちを口実にタスク全体を放棄しないが、権限回避や秘密の抜き取りは行わない。

既存変更の削除、force push、履歴改変、保護ブランチ回避、他プロジェクトの設定変更を行わない。自分の変更をcommit/pushし、リポジトリ規則に従って本番へ反映する。マージ権限がなければPRと実デプロイを残し、その状態を説明する。

## 10. 完了条件と最終報告

必須の完了条件：
[ ] v0.5.0の正しい取り込みと既存機能の回帰確認。
[ ] 審査員が開ける実在のVercel production URL。
[ ] 実OpenAIテキスト推論が契約と戦闘に反映される。
[ ] 実GPT-Live-1音声交渉、割り込み、言い直し、再交渉をproductionで実証。
[ ] 古い提案・二重イベント・曖昧な音声承諾による誤署名がない。
[ ] 音声なし・通信障害時にもローカルルールを明示して試遊を継続できる。
[ ] 秘密保護、分散クォータ、予算制限、音声の終了処理が検証されている。
[ ] 実施した環境・実機・エミュレーション・モックの範囲を明示した検証記録。
[ ] 実動作の60秒動画ファイルと、検証済みの閲覧URL。
[ ] 英文応募欄6項目、ライセンス・由来の記録、提出候補commitとdeploymentの記録。

最終報告は日本語で、production URL、repo/branch/commit、音声と契約で実際に使ったモデル、実API試験の結果、テスト結果、確認した端末、動画URL、Vercel/OpenAIそれぞれの使用量と上限、ロールバック方法を示す。

未達項目があれば「完了」と総括しない。できた範囲、観測した失敗、根拠、必要な本人操作を区別し、再開可能な状態を残す。実在しないURL、未実行のテスト、測っていないFPS、モックを実APIとする成功報告を出さない。

今すぐ、作業ツリー・v0.5.0入力・接続済みサービスを確認し、最初の実モデル契約がVercel上で戦闘に反映される縦切り実装から着手する。

## 一次資料（2026-09-14確認。実行時に再確認）

[S1] OpenAI, GPT-Live-1 API launch, 2026-09-10
https://openai.com/index/introducing-gpt-live-1-in-the-api/

[S2] OpenAI公式告知：接続方式、delegation、割り込みと取消、別建て料金
https://community.openai.com/t/introducing-gpt-live-1-in-the-api/1396471

[S3] Vercel, WebSocket Public Beta, 2026-06-22
https://vercel.com/changelog/websocket-support-is-now-in-public-beta

[S4] Vercel, WebSocket connections and Function lifetime, updated 2026-06-22
https://vercel.com/kb/guide/do-vercel-serverless-functions-support-websocket-connections

[S5] Vercel, Spend Management（プラン、対象費用、チーム全体への影響を確認）
https://vercel.com/docs/spend-management

[S6] OpenAI Live API, Hang up session（接続作成の仕様は別途公式ガイドで確認）
https://developers.openai.com/api/reference/typescript/resources/live/subresources/sessions/methods/hangup

[S7] 開始時に再確認する既存リポジトリの対象package.json
https://github.com/Yasei-no-otoko/openai-100-hour-game-builder-challenge/blob/main/NEMESIS-PACT-3D/package.json

補足：上記は方針を裏付ける一次資料であり、GPT-Live-1のブラウザ初期接続仕様をこのファイルで完全に検証済みという意味ではない。SDK・正式な初期化ガイドと対象プロジェクトの実アクセス確認を省略しない。
